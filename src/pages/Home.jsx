import React from 'react';
import { Form, Teams } from '../components';

const Home = () => {
	console.log('Home Rendered');
	return (
		<>
			<Form />
			<Teams />
		</>
	);
};

export default Home;
